[package]
name = "ralph_os"
version = "0.1.0"
edition = "2021"

# No external dependencies - everything is implemented from scratch

[dependencies]

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = 2
lto = true
